import React, { Component } from 'react';
import { Container, Header, Content, Card, CardItem, Body, Text } from 'native-base';
export default class PostCard extends Component {
  render() {
    return (
          <Card>
            <Text>This is a card! (WOW)</Text>
          </Card>
    );
  }
}